# PyQt5 窗口位置跳动问题分析：为什么 Labelme 启动后会从主屏跳到副屏

## 问题现象

Labelme 应用启动时：
1. 窗口首先显示在主显示屏上
2. 大约 2-3 秒后，窗口突然跳到了副显示屏

## 根本原因

这个问题的核心在于 **`QMainWindow.restoreState()`** 的行为。

### Qt 的窗口状态机制

在 PyQt5/Qt 中，`QMainWindow` 提供了两个关键方法：

- **`saveState()`**：保存主窗口的状态，返回 `QByteArray`
- **`restoreState(state)`**：从 QByteArray 恢复之前保存的状态

**关键认知误区**：很多人认为 `saveState()` 只保存工具栏和 Dock 的布局，但实际上它**也会保存窗口的位置和大小**！

### Labelme 的代码执行流程

在 `labelme/app.py` 的窗口初始化代码中（约第 1124-1135 行）：

```python
# 1. 从配置文件读取保存的状态
size = self.settings.value("window/size", QtCore.QSize(600, 500))
position = self.settings.value("window/position", QtCore.QPoint(0, 0))
state = self.settings.value("window/state", QtCore.QByteArray())

# 2. 调用 _adjustWindowPosition 将窗口定位到主屏幕中间
adjusted_position, adjusted_size = self._adjustWindowPosition(position, size)
self.move(adjusted_position)  # ← 窗口现在位于主屏幕

# 3. 调用 restoreState 恢复之前的状态
self.restoreState(state)  # ← 💥 问题在这里！这会覆盖之前的位置！
```

### 问题时间线

1. **第 1132 行**：`self.move(adjusted_position)` 将窗口移到主屏幕中间
2. **第 1135 行**：`self.restoreState(state)` 从 `labelme.ini` 读取之前保存的窗口状态
3. **结果**：如果上次关闭时窗口在副屏，`restoreState` 会将其移回副屏

### 为什么有 2-3 秒的延迟？

`restoreState` 的执行不是瞬时的，它需要：
1. 解析 QByteArray 中的状态数据
2. 重新布局所有工具栏和 Dock
3. 调整窗口位置

这个过程可能需要几帧的渲染时间，因此用户会看到窗口先出现在主屏，然后"跳"到副屏。

## 配置文件位置

窗口状态保存在应用目录的 `labelme.ini` 文件中：

```ini
[window]
position=@Point(2880 100)  ← 这里记录了副屏的坐标
size=@Size(1200 800)
state=@ByteArray(...)      ← 这里也包含位置信息
```

当主屏幕分辨率为 1920x1080，副屏从 x=1920 开始时，坐标 (2880, 100) 就在副屏上。

## 解决方案

### 方案 1：restoreState 后强制移动（推荐）

在调用 `restoreState` 后，再次将窗口移到主屏幕：

```python
self.restoreState(state)
# 强制移回主屏幕
main_screen = QtWidgets.QApplication.primaryScreen()
screen_geometry = main_screen.availableGeometry()
x = screen_geometry.center().x() - self.width() // 2
y = screen_geometry.center().y() - self.height() // 2
self.move(x, y)
```

### 方案 2：只恢复工具栏/Dock 状态

如果 `saveState` 保存的版本号不同，`restoreState` 会失败而不修改窗口位置：

```python
# 使用不同的版本号，使位置信息失效
self.restoreState(state, version=1)  # 保存时 version=0
```

但这可能影响其他状态的恢复。

### 方案 3：检测显示器变化

在恢复状态前，检查保存的位置是否在当前可用的屏幕范围内：

```python
def is_position_valid(pos):
    for screen in QtWidgets.QApplication.screens():
        if screen.geometry().contains(pos):
            return True
    return False

if not is_position_valid(position):
    # 位置无效，使用默认位置
    position = QtCore.QPoint(0, 0)
```

## 总结

| 项目 | 说明 |
|------|------|
| **罪魁祸首** | `QMainWindow.restoreState()` |
| **原因** | `restoreState` 会恢复窗口位置，覆盖手动设置的屏幕居中 |
| **触发条件** | 上次关闭时窗口在副屏，本次启动后显示器配置未变 |
| **修复位置** | `labelme/app.py` 第 1135 行之后 |

## 延伸知识：saveState 保存了什么？

根据 Qt 文档，`QMainWindow.saveState()` 保存：

1. **工具栏状态**：位置、大小、浮动状态、可见性
2. **Dock Widget 状态**：位置、大小、浮动状态、标签页顺序
3. **窗口状态**：最大化、最小化、位置、大小

注意：`saveGeometry()` 和 `saveState()` 都会保存位置信息，但 `saveGeometry` 更全面（包含框架大小等）。

## 参考

- [Qt 文档: QMainWindow::saveState](https://doc.qt.io/qt-5/qmainwindow.html#saveState)
- [Qt 文档: QMainWindow::restoreState](https://doc.qt.io/qt-5/qmainwindow.html#restoreState)
